
  # Criar app celular baseado em imagens

  This is a code bundle for Criar app celular baseado em imagens. The original project is available at https://www.figma.com/design/3Si2G7R4XwfBy9uDuNz093/Criar-app-celular-baseado-em-imagens.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  